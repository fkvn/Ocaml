(2 + 3) * (2 + 3);;

let x = 2 + 3 in
  x * x
;;

let y = 2 in
let squared = y * y in
let cubed = y * squared in
  squared * cubed
;;

let a = "a" in
let b = "b" in
let astr = a ^ a ^ a in
let bstr = b ^ b ^ b in
  astr ^ bstr
;;
